# I18N-1 — i18next Setup (EN default + PT-BR)

## Goal
Implement internationalization for the mobile app using i18next.

## Scope
- English as default language
- PT-BR as secondary language
- Auto-detect device language
- Allow manual language switching in Settings
- No hardcoded user-facing strings

## Constraints
- All UI strings must come from translation files
- Use a scalable structure for future languages

## Acceptance Criteria
- App renders in EN by default
- App switches to PT-BR if device language is Portuguese
- User can manually switch language in Settings
